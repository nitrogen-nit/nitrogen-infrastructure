discovery.docker "nitrogen" {
  host = "unix:///var/run/docker.sock"
}

discovery.relabel "nitrogen_logs" {
  targets = discovery.docker.nitrogen.targets

  rule {
    source_labels = ["__meta_docker_container_label_com_docker_compose_project"]
    regex         = "nitrogen-local|nitrogen-observability"
    action        = "keep"
  }

  rule {
    source_labels = ["__meta_docker_container_name"]
    regex         = "/?(.*)"
    target_label  = "container"
  }

  rule {
    source_labels = ["__meta_docker_container_label_com_docker_compose_service"]
    target_label  = "service"
  }

  rule {
    source_labels = ["__meta_docker_container_label_com_docker_compose_project"]
    target_label  = "compose_project"
  }
}

loki.source.docker "nitrogen" {
  host       = "unix:///var/run/docker.sock"
  targets    = discovery.relabel.nitrogen_logs.output
  labels     = {"job" = "docker"}
  forward_to = [loki.write.local.receiver]
}

loki.write "local" {
  endpoint {
    url = "http://loki:3100/loki/api/v1/push"
  }
}
